# PHP Backend (for WebSit/backend)

This folder contains a minimal PHP backend using SQLite.
See instructions in the root README or run locally:

```bash
php scripts/init_db.php
php -S 0.0.0.0:8000 -t public
```
